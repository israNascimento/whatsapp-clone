import { StyleSheet } from 'react-native';

const styles = StyleSheet.create(
  {
    errorMessage: {
      color: '#F00',
      fontSize: 15,
      fontWeight: 'bold',
      padding: 5,
    },
    input: {
      height: 45,
      fontSize: 20,
    },
  },
);

export default styles;
